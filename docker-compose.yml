services:
  frontend:
    build:
      context: ./booknest
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build:
      context: ./booknest-server
    ports:
      - "5000:5000"
    env_file:
      - ./booknest-server/.env
